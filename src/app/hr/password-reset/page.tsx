import { completeResetAction, requestResetAction } from "./actions";
export default async function ResetPage({ searchParams }: { searchParams: Promise<{ requested?: string; error?: string }> }) { const params = await searchParams; return <main className="min-h-screen bg-slate-100 px-4 py-16"><section className="mx-auto max-w-md rounded-3xl bg-white p-7"><h1 className="text-3xl font-bold">Password reset</h1>{params.requested ? <p className="mt-4 text-sm text-slate-700">If the account exists, reset instructions have been queued.</p> : null}{params.error ? <p role="alert" className="mt-4 text-red-700">The reset link is invalid or expired.</p> : null}<form action={requestResetAction} className="mt-6 space-y-3"><input className="input" name="email" type="email" placeholder="Work email" required /><button className="btn btn-secondary w-full">Request reset</button></form><hr className="my-6" /><form action={completeResetAction} className="space-y-3"><input className="input" name="password" type="password" placeholder="New strong password" required /><button className="btn btn-primary w-full">Set new password</button></form></section></main>; }
