import Link from "next/link";
import { hrLogoutAction } from "@/app/hr/actions";

export function HrPortalShell({ title, email, links, children }: { title: string; email: string; links: Array<[string, string]>; children: React.ReactNode }) {
  return <div className="min-h-screen bg-slate-100"><header className="border-b border-slate-200 bg-slate-950 text-white"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-4 py-5 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-teal-300">Zentric Analytics HRMS</p><p className="text-xl font-bold">{title}</p></div><div className="flex flex-wrap items-center gap-3 text-sm"><span>{email}</span><form action={hrLogoutAction}><button className="rounded-lg border border-white/30 px-3 py-2">Sign out</button></form></div></div><nav aria-label={`${title} navigation`} className="mx-auto flex max-w-7xl gap-1 overflow-x-auto px-4 pb-3">{links.map(([label, href]) => <Link className="whitespace-nowrap rounded-lg px-3 py-2 text-sm text-slate-200 hover:bg-white/10" href={href} key={href}>{label}</Link>)}</nav></header><main className="mx-auto max-w-7xl px-4 py-8">{children}</main></div>;
}
