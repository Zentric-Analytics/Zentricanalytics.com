import { describe, expect, it } from 'vitest';import { canDownloadDocument, generateApplicationId, maskSensitive } from '../src/lib/hiring';import { renderSubmittedDocumentText } from '../src/lib/pdf';
describe('hiring workflow helpers',()=>{it('generates formatted application ids',()=>{expect(generateApplicationId(41,new Date('2026-06-21T00:00:00Z'))).toBe('ZA-APP-2026-00041')});it('locks downloads until signed and submitted',()=>{expect(canDownloadDocument('Under Review',true,'2026-06-21')).toBe(true);expect(canDownloadDocument('Available',true,'2026-06-21')).toBe(false);expect(canDownloadDocument('Under Review',false,'2026-06-21')).toBe(false)});it('masks sensitive values in generated document text',()=>{const doc=renderSubmittedDocumentText({title:'Candidate Information',applicantName:'Ada',applicationId:'ZA-APP-2026-00041',fields:[{label:'ID number',value:'12345678901',sensitive:true}],signatureName:'Ada',submittedAt:'2026-06-21',version:1,status:'Submitted'});expect(doc).toContain('*******8901');expect(doc).not.toContain('12345678901')})});
