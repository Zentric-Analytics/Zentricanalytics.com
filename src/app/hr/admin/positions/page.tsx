import { prisma } from "@/lib/prisma";
import { requirePermission } from "@/lib/hr/permissions/authorize";
import { archivePositionAction, createPositionAction } from "./actions";

export default async function PositionsPage() {
  const auth = await requirePermission("position.manage");
  const [positions, departments] = await Promise.all([
    prisma.hrPosition.findMany({ where: { organizationId: auth.user.organizationId }, include: { department: true, team: true, _count: { select: { employeeAssignments: true } } }, orderBy: [{ status: "asc" }, { title: "asc" }] }),
    prisma.hrDepartment.findMany({ where: { organizationId: auth.user.organizationId, status: "ACTIVE" }, orderBy: { name: "asc" } }),
  ]);
  return <><h1 className="text-3xl font-bold">Positions</h1><p className="mt-2 text-slate-600">Define approved positions and salary bands. Salary bands do not expose employee pay.</p>
    <section className="mt-6 rounded-2xl bg-white p-5"><h2 className="font-bold">Create position</h2><form action={createPositionAction} className="mt-4 grid gap-3 md:grid-cols-4"><input className="input" name="code" placeholder="Code" required /><input className="input" name="title" placeholder="Title" required /><select className="input" name="departmentId" required><option value="">Department</option>{departments.map((department) => <option value={department.id} key={department.id}>{department.name}</option>)}</select><input className="input" name="currency" defaultValue="NGN" maxLength={3} required /><input className="input" name="salaryBandMinimum" type="number" min="0" step="0.01" placeholder="Band minimum" /><input className="input" name="salaryBandMaximum" type="number" min="0" step="0.01" placeholder="Band maximum" /><input className="input" name="description" placeholder="Description" /><button className="btn btn-primary">Create</button></form></section>
    <div className="mt-6 overflow-x-auto rounded-2xl bg-white"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-4">Code</th><th className="p-4">Position</th><th className="p-4">Department</th><th className="p-4">Salary band</th><th className="p-4">Assignments</th><th className="p-4">Status</th><th className="p-4">Action</th></tr></thead><tbody>{positions.map((position) => <tr className="border-b last:border-0" key={position.id}><td className="p-4 font-mono">{position.code}</td><td className="p-4 font-semibold">{position.title}</td><td className="p-4">{position.department.name}</td><td className="p-4">{position.salaryBandMinimum !== null || position.salaryBandMaximum !== null ? `${position.currency} ${position.salaryBandMinimum?.toString() ?? "—"} – ${position.salaryBandMaximum?.toString() ?? "—"}` : "Not set"}</td><td className="p-4">{position._count.employeeAssignments}</td><td className="p-4">{position.status}</td><td className="p-4">{position.status === "ACTIVE" ? <form action={archivePositionAction} className="flex gap-2"><input type="hidden" name="id" value={position.id} /><input className="input min-w-40" name="reason" placeholder="Archive reason" required /><button className="font-semibold text-red-700">Archive</button></form> : "History retained"}</td></tr>)}</tbody></table>{!positions.length ? <p className="p-6 text-slate-500">No positions yet.</p> : null}</div></>;
}
