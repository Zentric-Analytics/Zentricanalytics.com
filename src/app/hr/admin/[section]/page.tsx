import { notFound } from "next/navigation";
const sections = new Set(["employees","departments","positions","assignments","leave","payroll","documents","assets","workflows","audit","settings","supervisors","payslips"]);
export default async function FoundationSection({ params }: { params: Promise<{ section: string }> }) { const { section } = await params; if (!sections.has(section)) notFound(); const title = section[0].toUpperCase() + section.slice(1); return <><h1 className="text-3xl font-bold">{title}</h1><div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-white p-8"><h2 className="font-bold">Foundation ready</h2><p className="mt-2 text-slate-600">This module is scheduled for a later HRMS milestone. Authentication and permission boundaries are active.</p></div></>; }
