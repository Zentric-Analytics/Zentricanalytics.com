export function StatusBadge({status}:{status:string}){const tone=status.includes('Locked')?'bg-slate-100 text-slate-600':status.includes('Approved')||status.includes('Completed')||status.includes('Download')?'bg-emerald-50 text-emerald-700':status.includes('Correction')||status.includes('Rejected')?'bg-amber-50 text-amber-700':'bg-blue-50 text-blue-700';return <span className={`rounded-full px-3 py-1 text-xs font-bold ${tone}`}>{status}</span>}
