import { canDownloadDocument, type StageStatus } from '@/lib/hiring';import { StatusBadge } from './StatusBadge';
export function DocumentCard({title,status,signed,submittedAt}:{title:string;status:StageStatus;signed:boolean;submittedAt?:string|null}){const ok=canDownloadDocument(status,signed,submittedAt);return <article className="card p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{title}</h3><p className="mt-2 text-sm text-slate-600">{ok?'Signed copy is available for download.':'Download is locked until the form is electronically signed and submitted.'}</p></div><StatusBadge status={ok?'Download Available':status}/></div><button className="btn btn-secondary mt-4 disabled:opacity-50" disabled={!ok}>Download PDF</button></article>}
